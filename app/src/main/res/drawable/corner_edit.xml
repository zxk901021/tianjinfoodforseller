<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" />
    <corners android:radius="5dp" />
    <padding
        android:bottom="@dimen/corner_dimen"
        android:left="@dimen/corner_dimen"
        android:right="@dimen/corner_dimen"
        android:top="@dimen/corner_dimen" />

    <stroke android:color="@color/separate_line" android:width="1dp"/>

</shape>
